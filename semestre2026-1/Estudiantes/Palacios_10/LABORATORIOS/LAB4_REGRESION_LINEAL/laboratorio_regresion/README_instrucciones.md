# Librería de Regresión Lineal (Gradiente Descendente)

Esta librería permite ajustar modelos de regresión lineal simple utilizando el algoritmo de Gradiente Descendente.

## Instalación
Desde la carpeta raíz del proyecto, ejecuta en la terminal el siguiente comando para instalar la librería:

```bash
pip install -e .

---

### Cómo instalar la librería usuario externo

Ahora como un usuario externo. 

1. Abre la **Terminal** en VS Code (arriba en el menú: *Terminal -> New Terminal*).

2. Asegúrate de estar en la carpeta `\semestre2026-1\Estudiantes\Palacios_10\LABORATORIOS\LAB4_REGRESION_LINEAL\laboratorio_regresion`.

3. Escribe el siguiente comando en la terminal y presiona Enter:
   ```bash
   pip install -e .